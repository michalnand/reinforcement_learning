# set latexfile to the name of the main file without the .tex
latexfile = pycon2020_rl_pytorch

all :
	rm bin/$(latexfile).pdf -rf
	pdflatex $(latexfile).tex

	cp $(latexfile).pdf bin/.


clean:
	rm -f bin/$(latexfile).pdf
